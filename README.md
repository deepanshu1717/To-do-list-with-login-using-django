# To-do-list-with-login-using-django
add virtual environment with name (venv) to run this code

1. follow these steps
2. pip install virtualenv
3. virtualenv venv
4. venv\scripts\activate
them instal django with command -
 pip install django
